/**
 * Stub de `@/app/(app)/agentes/[slug]/actions` para o Storybook (ver
 * entity-actions.ts). O módulo real usa `node:fs` e não roda no build do Vite;
 * o alias em `.storybook/main.ts` o substitui por este stub.
 */
export async function saveAgent() {
  return { ok: true };
}
